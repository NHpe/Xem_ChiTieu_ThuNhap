

services:
  backend:
    build: ./backend
    container_name: expense_backend
    restart: always
    environment:
      - PORT=5000
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: expense_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend